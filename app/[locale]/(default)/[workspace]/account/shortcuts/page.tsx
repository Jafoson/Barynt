import { AccountShortcuts } from "@/features/account/components/AccountShortcuts/AccountShortcuts";

export const dynamic = "force-dynamic";

/** Reference list of the app's keyboard shortcuts — no data of its own. */
export default function AccountShortcutsPage() {
  return <AccountShortcuts />;
}
